// Import the images from the 'images' folder
// --- IMPORTANT: You must change the file names to match your images! ---
import Logo from './images/logo.png'; // <-- Check your logo file name
import BlurPanel from './images/BlurPanel.png'; // <-- Check your background file name

// Export them in the same "Assets" object structure
const Assets = {
  Images: {
    logo: Logo,
    loginLeftBG: BlurPanel
  }
};

export default Assets;